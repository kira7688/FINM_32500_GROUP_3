def realized_return(last_value: float, first_value: float): 
    
    
    realized_return = ((last_value-first_value)/(first_value))
    
    print(F"The realized return is {realized_return} in totality")
